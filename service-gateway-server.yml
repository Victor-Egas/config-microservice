server:
  port: 8090
 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
 cloud:
  gateway:
    routes:
      - id: service-customer
        uri: lb://service-customer
        predicates:
          - Path=/api/v1/customers/**
        filters:
          - StripPrefix=3
    #  - id: service-customer
     #   uri: lb://service-customer
      #  predicates:
       #   - path=/api/v1/customers/**
        #filters:
         # - StripPrefix=2
   